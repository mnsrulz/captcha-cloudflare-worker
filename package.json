{
  "name": "captcha-cloudflare-worker",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "build": "esbuild src/server.ts --bundle --minify --outfile=out/server.js --platform=node --format=esm",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tsx watch app.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.19.2",
    "tsx": "^3.12.9",
    "typescript": "^5.2.2"
  }
}
